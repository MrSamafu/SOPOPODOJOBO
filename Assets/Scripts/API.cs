using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class API : MonoBehaviour
{
    private BDD Bdd;

    // Start is called before the first frame update
    void Start()
    {
        Bdd = GetComponent<BDD>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
